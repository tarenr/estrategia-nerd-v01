ID: FIX-008
Tipo: Correcao (seguranca)
Nome: Isolamento de operacoes sobre stage/producao no ambiente local

## Objetivo

Corrigir o fato de que o painel /admin/* (rodando em qualquer ambiente
onde o codigo seja publicado) tinha rotas capazes de ler/escrever
diretamente nas bases e FTP de stage e producao, usando apenas
permissao de usuario (capability) como controle - sem nenhuma
checagem de qual maquina/ambiente estava executando o codigo.

## Contexto

Investigacao encontrou 4 pontos de acesso cruzado a ambientes dentro
do /admin/*, todos sem checagem de ambiente de execucao:

1. `POST /admin/central-operacional/sincronizar-conteudo` -
   `ContentSyncController::syncProductionToStage()` - exportava
   producao e aplicava em stage.
2. `/admin/central-operacional-v2/backup-sistemico` e
   `/backup-editorial` (`OperationsV2Controller`), alem das abas
   "Backup e Restore"/"Conteudo" da tela v1
   (`OperationsHubController`) - reaproveitavam os controllers de
   `/local/*` via `viewData()`, que pulava a checagem de ambiente
   local que so existia em `index()`/`handle()`.
3. `POST /admin/testes` (`AutomatedTestsController`) - aceitava
   `$_POST['environment']` livre, podendo rodar backup real e abrir
   PDO contra stage/producao.
4. As proprias rotas `/local/*` e `/dev` nao tinham checagem de
   ambiente no codigo - so uma regra escrita em
   `docs/CODEX-REGRAS-PERMANENTES.txt` (nao tecnica).

**Decisao final sobre os itens 1 e 2:** a primeira versao deste fix
removeu essas telas/rotas do `/admin/*` por completo. Apos alinhamento
com o usuario, a decisao foi revertida: essas ferramentas devem
continuar disponiveis dentro do admin **quando ele estiver rodando no
ambiente local**, e so ficar bloqueadas (404) se esse mesmo codigo
rodar em stage ou producao. Por isso os itens 1 e 2 foram restaurados
e protegidos com `EnvironmentGuard::requireLocal()` (explicitamente em
`ContentSyncController::syncProductionToStage()`; automaticamente nos
demais, que ja herdam a guarda de `viewData()`). O item 3
(`AutomatedTestsController`) nao foi alterado por essa reversao -
continua restrito por allowlist + `EnvironmentGuard::requireLocal()`
pra ambientes nao-locais, sem remover nada da tela.

Durante a implementacao, foi encontrado que `public/index.php` ja
tinha uma guarda central pra `/local/*` e `/dev` usando a classe
`LocalOnlyAccess` (`app/Support/LocalOnlyAccess.php`), com checagem
de `APP_ENV` + origem de rede. Essa logica foi fundida em
`EnvironmentGuard::requireLocal()`.

Revisao complementar via Codex CLI (leitura, ver secao "GPT como
parceiro de consultoria" do CLAUDE.md do projeto) identificou que o
guard dependia de `APP_ENV` estar configurado explicitamente em
stage/producao - se ausente, o default do projeto (`development` em
`config/app.php`) faria o guard tratar o servidor como local. Esse
default foi corrigido para `production` (fail-closed) como parte
deste mesmo fix.

## Arquivos alterados

- `config/app.php` (default de APP_ENV: development -> production)
- `app/Support/EnvironmentGuard.php` (guarda central fundida,
  criterios cumulativos, sem bypass por debug)
- `app/Support/LocalOnlyAccess.php` (apagado, absorvido pelo acima)
- `app/Controllers/Admin/ContentSyncController.php` (mantido; guarda
  `EnvironmentGuard::requireLocal()` adicionada em
  `syncProductionToStage()`)
- `app/Services/Admin/ContentSyncAdminService.php` (mantido, sem
  alteracao)
- `app/Views/admin/operations/index.php` (mantido, sem alteracao)
- `app/Views/admin/operations-v2/backup-sistemico.php` (mantido, sem
  alteracao)
- `app/Views/admin/operations-v2/backup-editorial.php` (mantido, sem
  alteracao)
- `app/Views/admin/operations-v2/partials/backup-editorial-content.php`
  (mantido, sem alteracao)
- `app/Controllers/Admin/AutomatedTestsController.php`
- `app/Controllers/Admin/OperationsHubController.php` (abas
  "Backup e Restore"/"Conteudo" mantidas, protegidas via `viewData()`)
- `app/Controllers/Admin/OperationsV2Controller.php` (paineis
  "Backup Sistemico"/"Backup Editorial" mantidos, protegidos via
  `viewData()`)
- `app/Controllers/Site/BackupToolsController.php`
- `app/Controllers/Site/CentralOperacionalController.php`
- `app/Controllers/Site/ContentSyncToolsController.php`
- `app/Controllers/Site/DevController.php`
- `app/Controllers/Site/LocalDocsController.php`
- `app/Controllers/Site/SearchConsoleMonitorController.php`
- `app/Services/Admin/Presenters/OperationsV2Presenter.php`
- `app/Services/Site/AutomatedTestService.php`
- `app/Views/admin/operations-v2/panel.php`
- `app/Views/components/admin/sidebar.php`
- `config/routes.php`
- `public/index.php`
- `scripts/backup.php`
- `scripts/content-sync.php`

## Banco de dados

- Mudanca de schema: nao
- Mudanca de dados: nao
- Script necessario: nao

## Dependencias

- Nenhuma dependencia nova.

## Impacto em producao

Medio. As telas de sincronizacao/backup continuam disponiveis dentro
do `/admin/*`, mas passam a responder 404 quando o codigo roda fora
do ambiente local (stage/producao) - antes respondiam normalmente em
qualquer lugar, bastando ter a capability de usuario. Nao afeta rotas
publicas do site nem o fluxo normal do admin de conteudo.

## Afeta rotas criticas

Nao (rotas publicas do site e fluxo normal de posts/links do admin
continuam identicos).

## Risco

Baixo a medio.
- Requer que `APP_ENV` esteja setado corretamente em stage/producao
  (agora com fallback seguro caso falte) - a protecao dos itens 1 e 2
  depende inteiramente disso, ja que as telas continuam existindo no
  admin.
- `app/Support/LocalOnlyAccess.php` continua removido (absorvido por
  `EnvironmentGuard`) - unico apagamento real que sobrevive nesta
  correcao; nao remove funcionalidade, so consolida a guarda.
- FTP nao faz troca atomica de arquivos; interrupcao durante o apply
  pode deixar codigo misturado.

## Validacao minima

- `php -l` em todos os arquivos PHP alterados.
- `php scripts/preflight-check.php`.
- Guarda testada isoladamente: `APP_ENV=production`,
  `APP_ENV=homolog` com `debug=true` -> bloqueados; `APP_ENV=development`
  (local) -> liberado.
- Smoke test local via `php -S` nas rotas `/`, `/local/*`, `/dev`,
  `/admin/*` - sem fatal error, comportamento de auth preservado.
- Apos apply em cada ambiente remoto (stage/producao): confirmar que
  `/admin/central-operacional/sincronizar-conteudo`,
  `/admin/central-operacional-v2/backup-sistemico` e
  `/backup-editorial` respondem 404 la (ja que nao sao local), e que
  continuam acessiveis normalmente no admin local.

## Status Stage/Producao

- Origem validada: nao validada.
- Paridade local -> stage: nao validada.
- Paridade stage -> pacote: nao validada.
- Producao: nao publicado.

## Observacoes

Investigacao e plano de publicacao revisados com apoio de consulta
somente-leitura ao Codex CLI (GPT), conforme a secao "GPT como
parceiro de consultoria" do `CLAUDE.md` deste conjunto de projetos.

O pacote de codigo gerado antes desta revisao
(`code_2026-09-13_16-14-27_670dd31`) ficou desatualizado - nao inclui
a restauracao dos itens 1 e 2. Um novo pacote precisa ser gerado
antes de qualquer publicacao em stage ou producao.

ID: FIX-002
Tipo: Correcao
Nome: Ajuste de encoding e preflight para pacote tecnico stage

Objetivo:
Corrigir textos administrativos com mojibake real e evitar falso positivo do preflight em dados minificados do editor HTML.

Contexto:
Ao preparar o pacote tecnico para stage, o preflight bloqueou a rotina por encontrar textos quebrados em PHP e um falso positivo `question-inside-word` dentro do bundle gerado `admin-post-html-editor.js`.

Arquivos alterados:
- app/Controllers/Admin/OperationsV2Controller.php
- app/Services/Site/CentralOperacionalService.php
- scripts/preflight-check.php

Banco de dados:
- Mudanca de schema: nao
- Mudanca de dados: nao
- Script necessario: nao

Dependencias:
- Rotina existente de preflight
- Bundle gerado do editor HTML

Impacto em producao: baixo
Afeta rotas criticas: nao

Risco:
Baixo. A alteracao corrige apenas textos quebrados em PHP e restringe o falso positivo do preflight ao arquivo gerado do editor. A checagem de mojibake, replacement-char e merge markers continua ativa.

Origem validada: local
Paridade local -> stage: nao validada
Paridade stage -> pacote: nao validada

Validado local: sim
Validado stage: nao

Apto para release: sim

Observacoes:
- O preflight continua escaneando o bundle gerado para mojibake e replacement-char.
- Apenas a regra `question-inside-word` foi ignorada para `public/assets/js/admin-post-html-editor.js`, pois os dados minificados do editor contem sequencias que simulam palavras com `?` sem representar texto de interface.
- A correcao foi necessaria antes de gerar o pacote tecnico para stage.
